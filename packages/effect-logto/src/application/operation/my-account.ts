/**
 * Generated by scripts/generate.ts from spec/openapi.json — do not edit by hand.
 */
import type * as Effect from 'effect/Effect'

import type { LogtoError, OperationConfig, WithOptionalResponse } from '../config.ts'
import type {
	AddMfaVerificationPayload,
	AddUserIdentitiesPayload,
	CreateOrReplaceTotpMfaVerificationPayload,
	DeleteSessionByIdParams,
	GetEnterpriseSsoIdentityAccessTokenResponse,
	GetGrantsParams,
	GetGrantsResponse,
	GetLogtoConfigResponse,
	GetMfaSettingsResponse,
	GetMfaVerificationsResponse,
	GetProfileResponse,
	GetSessionsResponse,
	GetSocialIdentityAccessTokenResponse,
	ReplaceIdentityPayload,
	UpdateLogtoConfigPayload,
	UpdateLogtoConfigResponse,
	UpdateMfaSettingsPayload,
	UpdateMfaSettingsResponse,
	UpdateMfaVerificationNamePayload,
	UpdateOtherProfilePayload,
	UpdateOtherProfileResponse,
	UpdatePasswordPayload,
	UpdatePrimaryEmailPayload,
	UpdatePrimaryPhonePayload,
	UpdateProfilePayload,
	UpdateProfileResponse,
	UpdateSocialIdentityAccessTokenByVerificationIdPayload,
	UpdateSocialIdentityAccessTokenByVerificationIdResponse,
	UploadAvatarFormData,
	UploadAvatarResponse,
} from '../../domain/schema/my-account.ts'

/**
 * Logto `My account` endpoints.
 */
export interface MyAccountOperations {
	/**
	 * Get profile for the user.
	 */
	readonly getProfile: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetProfileResponse, Config>, LogtoError>
	/**
	 * Update profile for the user, only the fields that are passed in will be updated. Updating or deleting username requires a logto-verification-id header for checking sensitive permissions. Removing any sign-in identifier, including username, is rejected if it would remove the user's last identifier.
	 */
	readonly updateProfile: <Config extends OperationConfig>(options: {
		readonly payload: UpdateProfilePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<UpdateProfileResponse, Config>, LogtoError>
	/**
	 * Update other profile for the user, only the fields that are passed in will be updated, to update the address, the user must have the address scope.
	 */
	readonly updateOtherProfile: <Config extends OperationConfig>(options: {
		readonly payload: UpdateOtherProfilePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<UpdateOtherProfileResponse, Config>, LogtoError>
	/**
	 * Update password for the user. A logto-verification-id header is required when the user has at least one security verification method: password, primary email, or primary phone. The header can be omitted only when the user has none of these methods and is setting the first password.
	 */
	readonly updatePassword: <Config extends OperationConfig>(options: {
		readonly payload: UpdatePasswordPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Get MFA settings for the user. This endpoint requires the Identities scope. Returns current MFA configuration preferences.
	 */
	readonly getMfaSettings: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetMfaSettingsResponse, Config>, LogtoError>
	/**
	 * Update MFA settings for the user. This endpoint requires identity verification and the Identities scope. Controls whether MFA verification is required during sign-in when the user has MFA configured.
	 */
	readonly updateMfaSettings: <Config extends OperationConfig>(options: {
		readonly payload: UpdateMfaSettingsPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<UpdateMfaSettingsResponse, Config>, LogtoError>
	/**
	 * Retrieve the exposed portion of the current user's logto config. This includes MFA states (enabled, skipped, skipMfaOnSignIn) and passkey sign-in binding states (skipped). Passkey is a WebAuthn MFA factor and shares the same account center field access control as MFA.
	 */
	readonly getLogtoConfig: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetLogtoConfigResponse, Config>, LogtoError>
	/**
	 * Update the exposed portion of the current user's logto config. Supports updating MFA states (enabled, skipped, skipMfaOnSignIn) and passkey sign-in binding states (skipped). Passkey is a WebAuthn MFA factor and shares the same account center field access control as MFA.
	 */
	readonly updateLogtoConfig: <Config extends OperationConfig>(options: {
		readonly payload: UpdateLogtoConfigPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<UpdateLogtoConfigResponse, Config>, LogtoError>
	/**
	 * This API retrieves the access token issued by a third-party social provider for a given social target.
	 * Access is only available if token storage is enabled for the corresponding social connector.
	 * When a user authenticates through a social provider, Logto automatically stores the provider’s tokens in an encrypted form.
	 * You can use this API to securely retrieve the stored access token and use it to access third-party APIs on behalf of the user.
	 */
	readonly getSocialIdentityAccessToken: <Config extends OperationConfig>(
		target: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetSocialIdentityAccessTokenResponse, Config>, LogtoError>
	/**
	 * This API updates the token storage for a social identity by a given social verification ID.
	 * It is used to fetch a new access token from the social provider and store it securely in Logto.
	 */
	readonly updateSocialIdentityAccessTokenByVerificationId: <Config extends OperationConfig>(
		target: string,
		options: { readonly payload: UpdateSocialIdentityAccessTokenByVerificationIdPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<UpdateSocialIdentityAccessTokenByVerificationIdResponse, Config>, LogtoError>
	/**
	 * This API retrieves the access token issued by a third-party enterprise SSO provider for a given SSO connector ID.
	 * Access is only available if token storage is enabled for the corresponding connector.
	 * When a user authenticates through a SSO provider, Logto automatically stores the provider’s tokens in an encrypted form.
	 * You can use this API to securely retrieve the stored access token and use it to access third-party APIs on behalf of the user.
	 */
	readonly getEnterpriseSsoIdentityAccessToken: <Config extends OperationConfig>(
		connectorId: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetEnterpriseSsoIdentityAccessTokenResponse, Config>, LogtoError>
	/**
	 * Update primary email for the user, a logto-verification-id in header is required for checking sensitive permissions, and a new identifier verification record is required for the new email ownership verification.
	 */
	readonly updatePrimaryEmail: <Config extends OperationConfig>(options: {
		readonly payload: UpdatePrimaryEmailPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Delete primary email for the user, a logto-verification-id header is required for checking sensitive permissions. The request is rejected if it would remove the user's last identifier.
	 */
	readonly deletePrimaryEmail: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Update primary phone for the user, a logto-verification-id in header is required for checking sensitive permissions, and a new identifier verification record is required for the new phone ownership verification.
	 */
	readonly updatePrimaryPhone: <Config extends OperationConfig>(options: {
		readonly payload: UpdatePrimaryPhonePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Delete primary phone for the user, a logto-verification-id header is required for checking sensitive permissions. The request is rejected if it would remove the user's last identifier.
	 */
	readonly deletePrimaryPhone: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Replace (upsert) an identity (social identity) for the user. Creates the identity if absent, replaces it otherwise. Idempotent. Unlike POST, this allows replacing an existing identity for the same social target. When replacing with a different social user ID, previously stored third-party tokens for the old identity are cleared. A logto-verification-id header is required when the user has at least one security verification method: password, primary email, or primary phone. The header can be omitted only when the user has none of these methods. A verification record for the new social identity is always required.
	 */
	readonly replaceIdentity: <Config extends OperationConfig>(options: {
		readonly payload: ReplaceIdentityPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Add an identity (social identity) to the user. A logto-verification-id header is required when the user has at least one security verification method: password, primary email, or primary phone. The header can be omitted only when the user has none of these methods. A verification record for the new social identity is always required.
	 */
	readonly addUserIdentities: <Config extends OperationConfig>(options: {
		readonly payload: AddUserIdentitiesPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Delete an identity (social identity) from the user. A logto-verification-id header is required when the user has at least one security verification method: password, primary email, or primary phone. The header can be omitted only when the user has none of these methods. The request is rejected if it would remove the user's last identifier.
	 */
	readonly deleteIdentity: <Config extends OperationConfig>(
		target: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Get MFA verifications for the user.
	 */
	readonly getMfaVerifications: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetMfaVerificationsResponse, Config>, LogtoError>
	/**
	 * Add a MFA verification to the user, a logto-verification-id in header is required for checking sensitive permissions.
	 */
	readonly addMfaVerification: <Config extends OperationConfig>(options: {
		readonly payload: AddMfaVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Create or replace the user's TOTP MFA verification with a new authenticator app binding. If the user already has a TOTP verification, it will be replaced; otherwise, a new one will be created. Requires a logto-verification-id header for sensitive permission checks, a valid TOTP secret, and a valid TOTP code generated from the secret.
	 */
	readonly createOrReplaceTotpMfaVerification: <Config extends OperationConfig>(options: {
		readonly payload: CreateOrReplaceTotpMfaVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Generate a TOTP secret for the user.
	 */
	readonly generateTotpSecret: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Generate backup codes for the user.
	 */
	readonly generateBackupCodes: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Get all backup codes for the user with their usage status. Requires identity verification.
	 */
	readonly getBackupCodes: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Update a MFA verification name, a logto-verification-id in header is required for checking sensitive permissions. Only WebAuthn is supported for now.
	 */
	readonly updateMfaVerificationName: <Config extends OperationConfig>(
		verificationId: string,
		options: { readonly payload: UpdateMfaVerificationNamePayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Delete an MFA verification, a logto-verification-id in header is required for checking sensitive permissions.
	 */
	readonly deleteMfaVerification: <Config extends OperationConfig>(
		verificationId: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Retrieve all non-expired sessions for the user, including session metadata and interaction details when available. A logto-verification-id in header is required for checking sensitive session details. Each entry includes an `isCurrent` boolean: `true` for the entry whose OIDC session backs the calling access token, `false` for the others. Use this to mark the "This device" entry in session-management UIs and to avoid revoking the caller's own session. At most one entry is `true` per response. Zero entries are tagged when the calling access token has no matching session uid — for example, the caller has revoked its own session but the token has not yet expired, or the token was issued from a non-session-backed grant.
	 */
	readonly getSessions: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetSessionsResponse, Config>, LogtoError>
	/**
	 * Revoke a specific user session by its ID, optionally revoking target associated grants and tokens.  A logto-verification-id in header is required for revoking sessions.
	 */
	readonly deleteSessionById: <Config extends OperationConfig>(
		sessionId: string,
		options: { readonly params?: DeleteSessionByIdParams | undefined; readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Retrieve all active application grants for the user. A logto-verification-id in header is required for checking grant details.
	 */
	readonly getGrants: <Config extends OperationConfig>(
		options: { readonly params?: GetGrantsParams | undefined; readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetGrantsResponse, Config>, LogtoError>
	/**
	 * Revoke a specific user application grant by grant ID and remove the related session authorization. A logto-verification-id in header is required for revoking grants.
	 */
	readonly deleteGrantById: <Config extends OperationConfig>(
		grantId: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Upload a single avatar image to the configured storage provider and return the public URL of the uploaded asset. Requires the `profile` scope and the `avatar` field to be editable in the account center configuration. The returned URL can then be used as the value for the avatar field via `PATCH /api/my-account`.
	 */
	readonly uploadAvatar: <Config extends OperationConfig>(options: {
		readonly payload: UploadAvatarFormData
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<UploadAvatarResponse, Config>, LogtoError>
}
