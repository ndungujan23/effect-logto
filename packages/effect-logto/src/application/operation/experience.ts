/**
 * Generated by scripts/generate.ts from spec/openapi.json — do not edit by hand.
 */
import type * as Effect from 'effect/Effect'

import type { LogtoError, OperationConfig, WithOptionalResponse } from '../config.ts'
import type {
	AddUserProfilePayload,
	BindMfaVerificationPayload,
	BindPasskeyPayload,
	CreateAndSendMfaVerificationCodePayload,
	CreateAndSendMfaVerificationCodeResponse,
	CreateAndSendVerificationCodePayload,
	CreateAndSendVerificationCodeResponse,
	CreateEnterpriseSsoVerificationPayload,
	CreateEnterpriseSsoVerificationResponse,
	CreateNewPasswordIdentityVerificationPayload,
	CreateNewPasswordIdentityVerificationResponse,
	CreatePasswordVerificationPayload,
	CreatePasswordVerificationResponse,
	CreateSignInPasskeyAuthenticationResponse,
	CreateSignInPasskeyAuthenticationWithIdentifierPayload,
	CreateSignInPasskeyAuthenticationWithIdentifierResponse,
	CreateSocialVerificationPayload,
	CreateSocialVerificationResponse,
	CreateTotpSecretResponse,
	CreateWebAuthnAuthenticationVerificationResponse,
	CreateWebAuthnRegistrationVerificationResponse,
	GenerateBackupCodesResponse,
	GetEnabledSsoConnectorsParams,
	GetEnabledSsoConnectorsResponse,
	GetInteractionResponse,
	IdentifyUserPayload,
	InitInteractionPayload,
	ResetUserPasswordPayload,
	SubmitInteractionResponse,
	UpdateInteractionEventPayload,
	UploadAvatarFormData,
	UploadAvatarResponse,
	VerifyBackupCodePayload,
	VerifyBackupCodeResponse,
	VerifyEnterpriseSsoVerificationPayload,
	VerifyEnterpriseSsoVerificationResponse,
	VerifyMfaVerificationCodePayload,
	VerifyMfaVerificationCodeResponse,
	VerifyOneTimeTokenVerificationPayload,
	VerifyOneTimeTokenVerificationResponse,
	VerifySignInPasskeyAuthenticationPayload,
	VerifySignInPasskeyAuthenticationResponse,
	VerifySocialVerificationPayload,
	VerifySocialVerificationResponse,
	VerifyTotpVerificationPayload,
	VerifyTotpVerificationResponse,
	VerifyVerificationCodeVerificationPayload,
	VerifyVerificationCodeVerificationResponse,
	VerifyWebAuthnAuthenticationVerificationPayload,
	VerifyWebAuthnAuthenticationVerificationResponse,
	VerifyWebAuthnRegistrationVerificationPayload,
	VerifyWebAuthnRegistrationVerificationResponse,
} from '../../domain/schema/experience.ts'

/**
 * Logto `Experience` endpoints.
 */
export interface ExperienceOperations {
	/**
	 * Init a new experience interaction with the given interaction type. Any existing interaction data will be cleared.
	 */
	readonly initInteraction: <Config extends OperationConfig>(options: {
		readonly payload: InitInteractionPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Update the current experience interaction event to the given event type. This API is used to switch the interaction event between `SignIn` and `Register`, while keeping all the verification records data.
	 */
	readonly updateInteractionEvent: <Config extends OperationConfig>(options: {
		readonly payload: UpdateInteractionEventPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * This API identifies the user based on the verificationId within the current experience interaction: <br/>- `SignIn` and `ForgotPassword` interactions: Verifies the user's identity using the provided `verificationId`. <br/>- `Register` interaction: Creates a new user account using the profile data from the current interaction. If a verificationId is provided, the profile data will first be updated with the verification record before creating the account. If not, the account is created directly from the stored profile data.
	 */
	readonly identifyUser: <Config extends OperationConfig>(options: {
		readonly payload: IdentifyUserPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Submit the current interaction. <br/>- Submit the verified user identity to the OIDC provider for further authentication (SignIn and Register). <br/>- Update the user's profile data if any (SignIn and Register). <br/>- Reset the password and clear all the interaction records (ForgotPassword).
	 */
	readonly submitInteraction: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<SubmitInteractionResponse, Config>, LogtoError>
	/**
	 * Get the public interaction data.
	 */
	readonly getInteraction: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetInteractionResponse, Config>, LogtoError>
	/**
	 * Create and verify a new Password verification record. The verification record can only be created if the provided user credentials are correct. If the password has expired under the password expiration policy, the request is rejected with `password.expired` and the user must reset their password before continuing.
	 */
	readonly createPasswordVerification: <Config extends OperationConfig>(options: {
		readonly payload: CreatePasswordVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<CreatePasswordVerificationResponse, Config>, LogtoError>
	/**
	 * Create a new `CodeVerification` record and sends the code to the specified identifier. The code verification can be used to verify the given identifier.
	 */
	readonly createAndSendVerificationCode: <Config extends OperationConfig>(options: {
		readonly payload: CreateAndSendVerificationCodePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<CreateAndSendVerificationCodeResponse, Config>, LogtoError>
	/**
	 * Verify the provided verification code against the user's identifier. If successful, the verification record will be marked as verified.
	 */
	readonly verifyVerificationCodeVerification: <Config extends OperationConfig>(options: {
		readonly payload: VerifyVerificationCodeVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyVerificationCodeVerificationResponse, Config>, LogtoError>
	/**
	 * Create a new MFA verification code and send it to the user's bound identifier (email or phone). This endpoint automatically uses the user's bound email address or phone number from their profile for MFA verification. The user must be identified before calling this endpoint.
	 */
	readonly createAndSendMfaVerificationCode: <Config extends OperationConfig>(options: {
		readonly payload: CreateAndSendMfaVerificationCodePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<CreateAndSendMfaVerificationCodeResponse, Config>, LogtoError>
	/**
	 * Verify the provided MFA verification code. The verification code must have been sent using the MFA verification code endpoint. This endpoint verifies the code against the user's bound identifier and marks the verification as complete if successful.
	 */
	readonly verifyMfaVerificationCode: <Config extends OperationConfig>(options: {
		readonly payload: VerifyMfaVerificationCodePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyMfaVerificationCodeResponse, Config>, LogtoError>
	/**
	 * Create a new SocialVerification record and return the provider's authorization URI for the given connector.
	 */
	readonly createSocialVerification: <Config extends OperationConfig>(
		connectorId: string,
		options: { readonly payload: CreateSocialVerificationPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<CreateSocialVerificationResponse, Config>, LogtoError>
	/**
	 * Verify the social authorization response data and get the user's identity data from the social provider.
	 */
	readonly verifySocialVerification: <Config extends OperationConfig>(
		connectorId: string,
		options: { readonly payload: VerifySocialVerificationPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<VerifySocialVerificationResponse, Config>, LogtoError>
	/**
	 * Create a new EnterpriseSSO verification record and return the provider's authorization URI for the given connector.
	 */
	readonly createEnterpriseSsoVerification: <Config extends OperationConfig>(
		connectorId: string,
		options: { readonly payload: CreateEnterpriseSsoVerificationPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<CreateEnterpriseSsoVerificationResponse, Config>, LogtoError>
	/**
	 * Verify the SSO authorization response data and get the user's identity from the SSO provider.
	 */
	readonly verifyEnterpriseSsoVerification: <Config extends OperationConfig>(
		connectorId: string,
		options: { readonly payload: VerifyEnterpriseSsoVerificationPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<VerifyEnterpriseSsoVerificationResponse, Config>, LogtoError>
	/**
	 * Create a new TOTP verification record and generate a new TOTP secret for the user. This secret can be used to bind a new TOTP verification to the user's profile. The verification record must be verified before the secret can be used to bind a new TOTP verification to the user's profile.
	 */
	readonly createTotpSecret: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<CreateTotpSecretResponse, Config>, LogtoError>
	/**
	 * Verifies the provided TOTP code against the new created TOTP secret or the existing TOTP secret. If a verificationId is provided, this API will verify the code against the TOTP secret that is associated with the verification record. Otherwise, a new TOTP verification record will be created and verified against the user's existing TOTP secret.
	 */
	readonly verifyTotpVerification: <Config extends OperationConfig>(options: {
		readonly payload: VerifyTotpVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyTotpVerificationResponse, Config>, LogtoError>
	/**
	 * Create a new WebAuthn registration verification record. The verification record can be used to bind a new WebAuthn credential to the user's profile.
	 */
	readonly createWebAuthnRegistrationVerification: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<CreateWebAuthnRegistrationVerificationResponse, Config>, LogtoError>
	/**
	 * Verify the WebAuthn registration response against the user's WebAuthn registration challenge. If the response is valid, the WebAuthn registration record will be marked as verified.
	 */
	readonly verifyWebAuthnRegistrationVerification: <Config extends OperationConfig>(options: {
		readonly payload: VerifyWebAuthnRegistrationVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyWebAuthnRegistrationVerificationResponse, Config>, LogtoError>
	/**
	 * Create a new WebAuthn authentication verification record based on the user's existing WebAuthn credential. This verification record can be used to verify the user's WebAuthn credential.
	 */
	readonly createWebAuthnAuthenticationVerification: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<CreateWebAuthnAuthenticationVerificationResponse, Config>, LogtoError>
	/**
	 * Verifies the WebAuthn authentication response against the user's authentication challenge. Upon successful verification, the verification record will be marked as verified.
	 */
	readonly verifyWebAuthnAuthenticationVerification: <Config extends OperationConfig>(options: {
		readonly payload: VerifyWebAuthnAuthenticationVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyWebAuthnAuthenticationVerificationResponse, Config>, LogtoError>
	/**
	 * Create WebAuthn authentication options for passkey sign-in with an identifier. The identifier is used to look up the user's WebAuthn credentials and generate non-discoverable authentication options.
	 */
	readonly createSignInPasskeyAuthenticationWithIdentifier: <Config extends OperationConfig>(options: {
		readonly payload: CreateSignInPasskeyAuthenticationWithIdentifierPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<CreateSignInPasskeyAuthenticationWithIdentifierResponse, Config>, LogtoError>
	/**
	 * Verify the passkey sign-in WebAuthn authentication response against the stored authentication challenge. When `verificationId` is provided, it verifies against the challenge generated by the identifier-based authentication endpoint. When omitted, it verifies against the preflight authentication options stored in the interaction. Upon successful verification, the verification record will be marked as verified and the user will be resolved by the credential if not provided earlier.
	 */
	readonly verifySignInPasskeyAuthentication: <Config extends OperationConfig>(options: {
		readonly payload: VerifySignInPasskeyAuthenticationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifySignInPasskeyAuthenticationResponse, Config>, LogtoError>
	/**
	 * Create a new BackupCode verification record with new backup codes generated. This verification record will be used to bind the backup codes to the user's profile.
	 */
	readonly generateBackupCodes: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GenerateBackupCodesResponse, Config>, LogtoError>
	/**
	 * Create a new BackupCode verification record and verify the provided backup code against the user's backup codes. The verification record will be marked as verified if the code is correct.
	 */
	readonly verifyBackupCode: <Config extends OperationConfig>(options: {
		readonly payload: VerifyBackupCodePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyBackupCodeResponse, Config>, LogtoError>
	/**
	 * Create a NewPasswordIdentity verification record for the new user registration use. The verification record includes a unique user identifier and a password that can be used to create a new user account.
	 */
	readonly createNewPasswordIdentityVerification: <Config extends OperationConfig>(options: {
		readonly payload: CreateNewPasswordIdentityVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<CreateNewPasswordIdentityVerificationResponse, Config>, LogtoError>
	/**
	 * Verify the provided one-time token against the user's email. If successful, the verification record will be marked as verified.
	 */
	readonly verifyOneTimeTokenVerification: <Config extends OperationConfig>(options: {
		readonly payload: VerifyOneTimeTokenVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<VerifyOneTimeTokenVerificationResponse, Config>, LogtoError>
	/**
	 * Adds user profile data to the current experience interaction. <br/>- For `Register`: The profile data provided before the identification request will be used to create a new user account. <br/>- For `SignIn` and `Register`: The profile data provided after the user is identified will be used to update the user's profile when the interaction is submitted. <br/>- `ForgotPassword`: Not supported.
	 */
	readonly addUserProfile: <Config extends OperationConfig>(options: {
		readonly payload: AddUserProfilePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Upload an image (e.g. avatar) during the `Register` experience interaction and return the public URL of the uploaded asset. The user account may not have been created yet at this point; the upload is scoped to the current interaction session. The returned URL can then be submitted as a profile field value via `POST /api/experience/profile`.
	 */
	readonly uploadAvatar: <Config extends OperationConfig>(options: {
		readonly payload: UploadAvatarFormData
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<UploadAvatarResponse, Config>, LogtoError>
	/**
	 * Reset the user's password. (`ForgotPassword` interaction only)
	 */
	readonly resetUserPassword: <Config extends OperationConfig>(options: {
		readonly payload: ResetUserPasswordPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Mark the user's MFA as enabled for the current interaction and persist in DB user configs upon successful submission.
	 */
	readonly markMfaEnabled: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Skip MFA verification binding flow. If the MFA is enabled in the sign-in experience settings and marked as `UserControlled`, the user can skip the MFA verification binding flow by calling this API.
	 */
	readonly skipMfaBindingFlow: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Mark the optional additional MFA binding suggestion as skipped for the current interaction. When multiple MFA factors are enabled and only an email/phone factor is configured, a suggestion to add another factor may be shown; this endpoint records the choice to skip.
	 */
	readonly skipMfaSuggestion: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Skip passkey binding flow. The users can temporarily skip the passkey binding flow by calling this API during sign-up. On sign-in, the skip flag will be persisted to user config.
	 */
	readonly skipPasskeyBinding: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Bind a WebAuthn credential as a passkey for sign-in purposes. Unlike `POST /api/experience/profile/mfa` with `type: WebAuthn`, this endpoint is exclusively for adding a passkey as a sign-in method and does NOT mark the user's optional MFA as enabled.
	 */
	readonly bindPasskey: <Config extends OperationConfig>(options: {
		readonly payload: BindPasskeyPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Bind new MFA verification to the user profile using the verificationId.
	 */
	readonly bindMfaVerification: <Config extends OperationConfig>(options: {
		readonly payload: BindMfaVerificationPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Extract the email domain from the provided email address. Returns all the enabled SSO connectors that match the email domain.
	 */
	readonly getEnabledSsoConnectors: <Config extends OperationConfig>(options: {
		readonly params: GetEnabledSsoConnectorsParams
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<GetEnabledSsoConnectorsResponse, Config>, LogtoError>
	/**
	 * Create WebAuthn authentication options for passkey sign-in. The user will be resolved later by the credential during verification.
	 */
	readonly createSignInPasskeyAuthentication: <Config extends OperationConfig>(
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<CreateSignInPasskeyAuthenticationResponse, Config>, LogtoError>
}
