/**
 * Generated by scripts/generate.ts from spec/openapi.json — do not edit by hand.
 */
import type * as Effect from 'effect/Effect'

import type { LogtoError, OperationConfig, WithOptionalResponse } from '../config.ts'
import type {
	CreatePayload,
	CreateResponse,
	CreateSigningKeyPayload,
	CreateSigningKeyResponse,
	GetResponse,
	ListParams,
	ListResponse,
	ListSigningKeysResponse,
	UpdatePayload,
	UpdateResponse,
	UpdateSigningKeyPayload,
	UpdateSigningKeyResponse,
} from '../../domain/schema/sso-connectors.ts'

/**
 * Logto `SSO connectors` endpoints.
 */
export interface SsoConnectorsOperations {
	/**
	 * Get SSO connectors with pagination. In addition to the raw SSO connector data, a copy of fetched or parsed IdP configs and a copy of connector provider's data will be attached.
	 */
	readonly list: <Config extends OperationConfig>(
		options: { readonly params?: ListParams | undefined; readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<ListResponse, Config>, LogtoError>
	/**
	 * Create an new SSO connector instance for a given provider.
	 */
	readonly create: <Config extends OperationConfig>(options: {
		readonly payload: CreatePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<CreateResponse, Config>, LogtoError>
	/**
	 * Get SSO connector data by ID. In addition to the raw SSO connector data, a copy of fetched or parsed IdP configs and a copy of connector provider's data will be attached.
	 */
	readonly get: <Config extends OperationConfig>(
		id: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetResponse, Config>, LogtoError>
	/**
	 * Delete an SSO connector by ID.
	 */
	readonly delete: <Config extends OperationConfig>(
		id: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Update an SSO connector by ID. This method performs a partial update.
	 */
	readonly update: <Config extends OperationConfig>(
		id: string,
		options: { readonly payload: UpdatePayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<UpdateResponse, Config>, LogtoError>
	/**
	 * List the Service Provider signing keys of the given SAML SSO connector. The response contains the certificate, fingerprints, activation state and expiry of each key — the private key is never returned.
	 */
	readonly listSigningKeys: <Config extends OperationConfig>(
		id: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<ListSigningKeysResponse, Config>, LogtoError>
	/**
	 * Generate a new Service Provider signing key pair for the given SAML SSO connector. Create the key inactive (default) to stage a graceful rotation — register its certificate at the identity provider before activating it. Creating an active key deactivates the currently active key atomically.
	 */
	readonly createSigningKey: <Config extends OperationConfig>(
		id: string,
		options: { readonly payload: CreateSigningKeyPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<CreateSigningKeyResponse, Config>, LogtoError>
	/**
	 * Delete the given Service Provider signing key. The active key cannot be deleted — activate a replacement key first, or disable signed authentication requests and deactivate this key.
	 */
	readonly deleteSigningKey: <Config extends OperationConfig>(
		id: string,
		keyId: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Update the activation state of the given Service Provider signing key. Activating a key atomically deactivates the currently active key — the switch step of a graceful rotation. Deactivating the active key is rejected while signed authentication requests are enabled — disable them first (key routes never modify the connector config).
	 */
	readonly updateSigningKey: <Config extends OperationConfig>(
		id: string,
		keyId: string,
		options: { readonly payload: UpdateSigningKeyPayload; readonly config?: Config | undefined }
	) => Effect.Effect<WithOptionalResponse<UpdateSigningKeyResponse, Config>, LogtoError>
}
