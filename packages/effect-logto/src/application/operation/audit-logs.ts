/**
 * Generated by scripts/generate.ts from spec/openapi.json — do not edit by hand.
 */
import type * as Effect from 'effect/Effect'

import type { LogtoError, OperationConfig, WithOptionalResponse } from '../config.ts'
import type { GetResponse, ListParams, ListResponse } from '../../domain/schema/audit-logs.ts'

/**
 * Logto `Audit logs` endpoints.
 */
export interface AuditLogsOperations {
	/**
	 * Get logs that match the given query with pagination.
	 *
	 * ### Pagination on large result sets
	 *
	 * For tenants with very large log volumes, the default exact `count(*)` may exceed Postgres `statement_timeout`. Pass `enableCap=true` to short-circuit the count query: any tenant with more than 10,000 matching logs saturates `Total-Number` at the sentinel value `10001` and the response includes `Total-Number-Is-Capped: true`. In capped responses the `Link` header omits `rel="last"` and `rel="next"`; walk forward by incrementing `page` and stop on an empty response.
	 */
	readonly list: <Config extends OperationConfig>(
		options: { readonly params?: ListParams | undefined; readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<ListResponse, Config>, LogtoError>
	/**
	 * Get log details by ID.
	 */
	readonly get: <Config extends OperationConfig>(
		id: string,
		options: { readonly config?: Config | undefined } | undefined
	) => Effect.Effect<WithOptionalResponse<GetResponse, Config>, LogtoError>
}
