/**
 * Generated by scripts/generate.ts from spec/openapi.json — do not edit by hand.
 */
import type * as Effect from 'effect/Effect'

import type { LogtoError, OperationConfig, WithOptionalResponse } from '../config.ts'
import type { CreatePayload, VerifyPayload } from '../../domain/schema/verification-codes.ts'

/**
 * Logto `Verification codes` endpoints.
 */
export interface VerificationCodesOperations {
	/**
	 * Request a verification code for the provided identifier (email/phone).
	 * if you're using email as the identifier, you need to setup your email connector first.
	 * if you're using phone as the identifier, you need to setup your SMS connector first.
	 */
	readonly create: <Config extends OperationConfig>(options: {
		readonly payload: CreatePayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
	/**
	 * Verify a verification code for a specified identifier.
	 * if you're using email as the identifier, you need to setup your email connector first.
	 * if you're using phone as the identifier, you need to setup your SMS connector first.
	 */
	readonly verify: <Config extends OperationConfig>(options: {
		readonly payload: VerifyPayload
		readonly config?: Config | undefined
	}) => Effect.Effect<WithOptionalResponse<void, Config>, LogtoError>
}
