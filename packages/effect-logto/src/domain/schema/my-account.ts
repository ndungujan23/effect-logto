/**
 * Generated by scripts/generate.ts from spec/openapi.json — do not edit by hand.
 */
import * as Schema from 'effect/Schema'

export type GetProfileResponse = {
	readonly id?: string
	readonly username?: string | null
	readonly primaryEmail?: string | null
	readonly primaryPhone?: string | null
	readonly name?: string | null
	readonly avatar?: string | null
	readonly customData?: { readonly [x: string]: Schema.Json }
	readonly identities?: { readonly [x: string]: { readonly userId: string; readonly details?: { readonly [x: string]: Schema.Json } } }
	readonly lastSignInAt?: number | null
	readonly createdAt?: number
	readonly updatedAt?: number
	readonly profile?: {
		readonly familyName?: string
		readonly givenName?: string
		readonly middleName?: string
		readonly nickname?: string
		readonly preferredUsername?: string
		readonly profile?: string
		readonly website?: string
		readonly gender?: string
		readonly birthdate?: string
		readonly zoneinfo?: string
		readonly locale?: string
		readonly address?: {
			readonly formatted?: string
			readonly streetAddress?: string
			readonly locality?: string
			readonly region?: string
			readonly postalCode?: string
			readonly country?: string
		}
	}
	readonly applicationId?: string | null
	readonly cimdClientId?: string | null
	readonly isSuspended?: boolean
	readonly hasPassword?: boolean
	readonly hasSecurityVerificationMethod?: boolean
	readonly ssoIdentities?: ReadonlyArray<{
		readonly tenantId: string
		readonly id: string
		readonly userId: string
		readonly issuer: string
		readonly identityId: string
		readonly detail: { readonly [x: string]: Schema.Json }
		readonly createdAt: number
		readonly updatedAt: number
		readonly ssoConnectorId: string
	}>
}
export const GetProfileResponse = Schema.Struct({
	id: Schema.optionalKey(
		Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
			Schema.isMaxLength(12).annotate({ expected: 'a value with a length of at most 12' })
		)
	),
	username: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	primaryEmail: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	primaryPhone: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	name: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	avatar: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(2048).annotate({ expected: 'a value with a length of at most 2048' })),
			Schema.Null,
		])
	),
	customData: Schema.optionalKey(
		Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({ description: 'arbitrary' })
	),
	identities: Schema.optionalKey(
		Schema.Record(
			Schema.String,
			Schema.Struct({
				userId: Schema.String,
				details: Schema.optionalKey(
					Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({ description: 'arbitrary' })
				),
			})
		)
	),
	lastSignInAt: Schema.optionalKey(
		Schema.Union([Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })), Schema.Null])
	),
	createdAt: Schema.optionalKey(Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' }))),
	updatedAt: Schema.optionalKey(Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' }))),
	profile: Schema.optionalKey(
		Schema.Struct({
			familyName: Schema.optionalKey(Schema.String),
			givenName: Schema.optionalKey(Schema.String),
			middleName: Schema.optionalKey(Schema.String),
			nickname: Schema.optionalKey(Schema.String),
			preferredUsername: Schema.optionalKey(Schema.String),
			profile: Schema.optionalKey(Schema.String),
			website: Schema.optionalKey(Schema.String),
			gender: Schema.optionalKey(Schema.String),
			birthdate: Schema.optionalKey(Schema.String),
			zoneinfo: Schema.optionalKey(Schema.String),
			locale: Schema.optionalKey(Schema.String),
			address: Schema.optionalKey(
				Schema.Struct({
					formatted: Schema.optionalKey(Schema.String),
					streetAddress: Schema.optionalKey(Schema.String),
					locality: Schema.optionalKey(Schema.String),
					region: Schema.optionalKey(Schema.String),
					postalCode: Schema.optionalKey(Schema.String),
					country: Schema.optionalKey(Schema.String),
				})
			),
		})
	),
	applicationId: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(21).annotate({ expected: 'a value with a length of at most 21' })),
			Schema.Null,
		])
	),
	cimdClientId: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(2048).annotate({ expected: 'a value with a length of at most 2048' })),
			Schema.Null,
		]).annotate({
			description:
				"The identifier of the CIMD (client ID metadata document) client from the user's first consent. `null` unless the user's first consent was granted to a CIMD client. Mutually exclusive with `applicationId`. Only returned with the `profile` scope.",
		})
	),
	isSuspended: Schema.optionalKey(Schema.Boolean),
	hasPassword: Schema.optionalKey(Schema.Boolean),
	hasSecurityVerificationMethod: Schema.optionalKey(Schema.Boolean),
	ssoIdentities: Schema.optionalKey(
		Schema.Array(
			Schema.Struct({
				tenantId: Schema.String.check(Schema.isMaxLength(21).annotate({ expected: 'a value with a length of at most 21' })),
				id: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(21).annotate({ expected: 'a value with a length of at most 21' })
				),
				userId: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(12).annotate({ expected: 'a value with a length of at most 12' })
				),
				issuer: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(256).annotate({ expected: 'a value with a length of at most 256' })
				),
				identityId: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })
				),
				detail: Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({
					description: 'arbitrary',
				}),
				createdAt: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				updatedAt: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				ssoConnectorId: Schema.String.check(
					Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })
				).check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			})
		)
	),
})

export type UpdateProfilePayload = {
	readonly name?: string | null
	readonly avatar?: string | null
	readonly username?: string | null
	readonly customData?: { readonly [x: string]: Schema.Json }
}
export const UpdateProfilePayload = Schema.Struct({
	name: Schema.optionalKey(Schema.Union([Schema.String, Schema.Null]).annotate({ description: 'The new name for the user.' })),
	avatar: Schema.optionalKey(
		Schema.Union([Schema.String, Schema.Null]).annotate({ description: 'The new avatar for the user, must be a URL.', format: 'url' })
	),
	username: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isPattern(/^[A-Z_a-z]\w*$/).annotate({ expected: 'a string matching the RegExp ^[A-Z_a-z]\\w*$' })),
			Schema.Null,
		]).annotate({ description: 'The new username for the user, must be a valid username and unique.', format: 'regex' })
	),
	customData: Schema.optionalKey(
		Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({
			description:
				'The new custom data for the user. This will completely replace the existing customData. Requires CustomData scope.',
		})
	),
})

export type UpdateProfileResponse = {
	readonly id?: string
	readonly username?: string | null
	readonly primaryEmail?: string | null
	readonly primaryPhone?: string | null
	readonly name?: string | null
	readonly avatar?: string | null
	readonly customData?: { readonly [x: string]: Schema.Json }
	readonly identities?: { readonly [x: string]: { readonly userId: string; readonly details?: { readonly [x: string]: Schema.Json } } }
	readonly lastSignInAt?: number | null
	readonly createdAt?: number
	readonly updatedAt?: number
	readonly profile?: {
		readonly familyName?: string
		readonly givenName?: string
		readonly middleName?: string
		readonly nickname?: string
		readonly preferredUsername?: string
		readonly profile?: string
		readonly website?: string
		readonly gender?: string
		readonly birthdate?: string
		readonly zoneinfo?: string
		readonly locale?: string
		readonly address?: {
			readonly formatted?: string
			readonly streetAddress?: string
			readonly locality?: string
			readonly region?: string
			readonly postalCode?: string
			readonly country?: string
		}
	}
	readonly applicationId?: string | null
	readonly cimdClientId?: string | null
	readonly isSuspended?: boolean
	readonly hasPassword?: boolean
	readonly hasSecurityVerificationMethod?: boolean
	readonly ssoIdentities?: ReadonlyArray<{
		readonly tenantId: string
		readonly id: string
		readonly userId: string
		readonly issuer: string
		readonly identityId: string
		readonly detail: { readonly [x: string]: Schema.Json }
		readonly createdAt: number
		readonly updatedAt: number
		readonly ssoConnectorId: string
	}>
}
export const UpdateProfileResponse = Schema.Struct({
	id: Schema.optionalKey(
		Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
			Schema.isMaxLength(12).annotate({ expected: 'a value with a length of at most 12' })
		)
	),
	username: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	primaryEmail: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	primaryPhone: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	name: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			Schema.Null,
		])
	),
	avatar: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(2048).annotate({ expected: 'a value with a length of at most 2048' })),
			Schema.Null,
		])
	),
	customData: Schema.optionalKey(
		Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({ description: 'arbitrary' })
	),
	identities: Schema.optionalKey(
		Schema.Record(
			Schema.String,
			Schema.Struct({
				userId: Schema.String,
				details: Schema.optionalKey(
					Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({ description: 'arbitrary' })
				),
			})
		)
	),
	lastSignInAt: Schema.optionalKey(
		Schema.Union([Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })), Schema.Null])
	),
	createdAt: Schema.optionalKey(Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' }))),
	updatedAt: Schema.optionalKey(Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' }))),
	profile: Schema.optionalKey(
		Schema.Struct({
			familyName: Schema.optionalKey(Schema.String),
			givenName: Schema.optionalKey(Schema.String),
			middleName: Schema.optionalKey(Schema.String),
			nickname: Schema.optionalKey(Schema.String),
			preferredUsername: Schema.optionalKey(Schema.String),
			profile: Schema.optionalKey(Schema.String),
			website: Schema.optionalKey(Schema.String),
			gender: Schema.optionalKey(Schema.String),
			birthdate: Schema.optionalKey(Schema.String),
			zoneinfo: Schema.optionalKey(Schema.String),
			locale: Schema.optionalKey(Schema.String),
			address: Schema.optionalKey(
				Schema.Struct({
					formatted: Schema.optionalKey(Schema.String),
					streetAddress: Schema.optionalKey(Schema.String),
					locality: Schema.optionalKey(Schema.String),
					region: Schema.optionalKey(Schema.String),
					postalCode: Schema.optionalKey(Schema.String),
					country: Schema.optionalKey(Schema.String),
				})
			),
		})
	),
	applicationId: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(21).annotate({ expected: 'a value with a length of at most 21' })),
			Schema.Null,
		])
	),
	cimdClientId: Schema.optionalKey(
		Schema.Union([
			Schema.String.check(Schema.isMaxLength(2048).annotate({ expected: 'a value with a length of at most 2048' })),
			Schema.Null,
		]).annotate({
			description:
				"The identifier of the CIMD (client ID metadata document) client from the user's first consent. `null` unless the user's first consent was granted to a CIMD client. Mutually exclusive with `applicationId`. Only returned with the `profile` scope.",
		})
	),
	isSuspended: Schema.optionalKey(Schema.Boolean),
	hasPassword: Schema.optionalKey(Schema.Boolean),
	hasSecurityVerificationMethod: Schema.optionalKey(Schema.Boolean),
	ssoIdentities: Schema.optionalKey(
		Schema.Array(
			Schema.Struct({
				tenantId: Schema.String.check(Schema.isMaxLength(21).annotate({ expected: 'a value with a length of at most 21' })),
				id: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(21).annotate({ expected: 'a value with a length of at most 21' })
				),
				userId: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(12).annotate({ expected: 'a value with a length of at most 12' })
				),
				issuer: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(256).annotate({ expected: 'a value with a length of at most 256' })
				),
				identityId: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })).check(
					Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })
				),
				detail: Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' })).annotate({
					description: 'arbitrary',
				}),
				createdAt: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				updatedAt: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				ssoConnectorId: Schema.String.check(
					Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })
				).check(Schema.isMaxLength(128).annotate({ expected: 'a value with a length of at most 128' })),
			})
		)
	),
})

export type UpdateOtherProfilePayload = {
	readonly familyName?: string
	readonly givenName?: string
	readonly middleName?: string
	readonly nickname?: string
	readonly preferredUsername?: string
	readonly profile?: string
	readonly website?: string
	readonly gender?: string
	readonly birthdate?: string
	readonly zoneinfo?: string
	readonly locale?: string
	readonly address?: {
		readonly formatted?: string
		readonly streetAddress?: string
		readonly locality?: string
		readonly region?: string
		readonly postalCode?: string
		readonly country?: string
	}
}
export const UpdateOtherProfilePayload = Schema.Struct({
	familyName: Schema.optionalKey(Schema.String.annotate({ description: 'The new family name for the user.' })),
	givenName: Schema.optionalKey(Schema.String.annotate({ description: 'The new given name for the user.' })),
	middleName: Schema.optionalKey(Schema.String.annotate({ description: 'The new middle name for the user.' })),
	nickname: Schema.optionalKey(Schema.String.annotate({ description: 'The new nickname for the user.' })),
	preferredUsername: Schema.optionalKey(Schema.String.annotate({ description: 'The new preferred username for the user.' })),
	profile: Schema.optionalKey(Schema.String.annotate({ description: 'The new profile for the user.' })),
	website: Schema.optionalKey(Schema.String.annotate({ description: 'The new website for the user.' })),
	gender: Schema.optionalKey(Schema.String.annotate({ description: 'The new gender for the user.' })),
	birthdate: Schema.optionalKey(Schema.String.annotate({ description: 'The new birthdate for the user.' })),
	zoneinfo: Schema.optionalKey(Schema.String.annotate({ description: 'The new zoneinfo for the user.' })),
	locale: Schema.optionalKey(Schema.String.annotate({ description: 'The new locale for the user.' })),
	address: Schema.optionalKey(
		Schema.Struct({
			formatted: Schema.optionalKey(Schema.String),
			streetAddress: Schema.optionalKey(Schema.String),
			locality: Schema.optionalKey(Schema.String),
			region: Schema.optionalKey(Schema.String),
			postalCode: Schema.optionalKey(Schema.String),
			country: Schema.optionalKey(Schema.String),
		}).annotate({ description: 'The new address for the user.' })
	),
})

export type UpdateOtherProfileResponse = {
	readonly familyName?: string
	readonly givenName?: string
	readonly middleName?: string
	readonly nickname?: string
	readonly preferredUsername?: string
	readonly profile?: string
	readonly website?: string
	readonly gender?: string
	readonly birthdate?: string
	readonly zoneinfo?: string
	readonly locale?: string
	readonly address?: {
		readonly formatted?: string
		readonly streetAddress?: string
		readonly locality?: string
		readonly region?: string
		readonly postalCode?: string
		readonly country?: string
	}
}
export const UpdateOtherProfileResponse = Schema.Struct({
	familyName: Schema.optionalKey(Schema.String),
	givenName: Schema.optionalKey(Schema.String),
	middleName: Schema.optionalKey(Schema.String),
	nickname: Schema.optionalKey(Schema.String),
	preferredUsername: Schema.optionalKey(Schema.String),
	profile: Schema.optionalKey(Schema.String),
	website: Schema.optionalKey(Schema.String),
	gender: Schema.optionalKey(Schema.String),
	birthdate: Schema.optionalKey(Schema.String),
	zoneinfo: Schema.optionalKey(Schema.String),
	locale: Schema.optionalKey(Schema.String),
	address: Schema.optionalKey(
		Schema.Struct({
			formatted: Schema.optionalKey(Schema.String),
			streetAddress: Schema.optionalKey(Schema.String),
			locality: Schema.optionalKey(Schema.String),
			region: Schema.optionalKey(Schema.String),
			postalCode: Schema.optionalKey(Schema.String),
			country: Schema.optionalKey(Schema.String),
		})
	),
})

export type UpdatePasswordPayload = { readonly password: string }
export const UpdatePasswordPayload = Schema.Struct({
	password: Schema.String.annotate({ description: 'The new password for the user.' }).check(
		Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })
	),
})

export type GetMfaSettingsResponse = { readonly skipMfaOnSignIn: boolean }
export const GetMfaSettingsResponse = Schema.Struct({ skipMfaOnSignIn: Schema.Boolean })

export type UpdateMfaSettingsPayload = { readonly skipMfaOnSignIn: boolean }
export const UpdateMfaSettingsPayload = Schema.Struct({ skipMfaOnSignIn: Schema.Boolean })

export type UpdateMfaSettingsResponse = { readonly skipMfaOnSignIn: boolean }
export const UpdateMfaSettingsResponse = Schema.Struct({ skipMfaOnSignIn: Schema.Boolean })

export type GetLogtoConfigResponse = {
	readonly mfa: { readonly enabled?: boolean; readonly skipped: boolean; readonly skipMfaOnSignIn: boolean }
	readonly passkeySignIn: { readonly skipped: boolean }
}
export const GetLogtoConfigResponse = Schema.Struct({
	mfa: Schema.Struct({ enabled: Schema.optionalKey(Schema.Boolean), skipped: Schema.Boolean, skipMfaOnSignIn: Schema.Boolean }),
	passkeySignIn: Schema.Struct({ skipped: Schema.Boolean }),
})

export type UpdateLogtoConfigPayload = {
	readonly mfa?: {
		readonly enabled?: boolean
		readonly skipped?: boolean
		readonly additionalBindingSuggestionSkipped?: boolean
		readonly skipMfaOnSignIn?: boolean
	}
	readonly passkeySignIn?: { readonly skipped?: boolean }
}
export const UpdateLogtoConfigPayload = Schema.Struct({
	mfa: Schema.optionalKey(
		Schema.Struct({
			enabled: Schema.optionalKey(Schema.Boolean.annotate({ description: 'Set whether MFA is enabled for the user.' })),
			skipped: Schema.optionalKey(
				Schema.Boolean.annotate({ description: 'Set whether the user is marked as having skipped MFA binding.' })
			),
			additionalBindingSuggestionSkipped: Schema.optionalKey(Schema.Boolean),
			skipMfaOnSignIn: Schema.optionalKey(
				Schema.Boolean.annotate({
					description:
						'Set whether the user has opted to skip MFA verification on sign-in. This is ignored when the MFA policy is mandatory.',
				})
			),
		})
	),
	passkeySignIn: Schema.optionalKey(
		Schema.Struct({
			skipped: Schema.optionalKey(
				Schema.Boolean.annotate({ description: 'Set whether the user has persistently skipped binding a passkey for sign-in.' })
			),
		})
	),
})

export type UpdateLogtoConfigResponse = {
	readonly mfa: { readonly enabled?: boolean; readonly skipped: boolean; readonly skipMfaOnSignIn: boolean }
	readonly passkeySignIn: { readonly skipped: boolean }
}
export const UpdateLogtoConfigResponse = Schema.Struct({
	mfa: Schema.Struct({ enabled: Schema.optionalKey(Schema.Boolean), skipped: Schema.Boolean, skipMfaOnSignIn: Schema.Boolean }),
	passkeySignIn: Schema.Struct({ skipped: Schema.Boolean }),
})

export type GetSocialIdentityAccessTokenResponse = {
	readonly access_token: string
	readonly scope?: string
	readonly token_type?: string
	readonly expires_in?: number | string
}
export const GetSocialIdentityAccessTokenResponse = Schema.Struct({
	access_token: Schema.String,
	scope: Schema.optionalKey(Schema.String),
	token_type: Schema.optionalKey(Schema.String),
	expires_in: Schema.optionalKey(
		Schema.Union([Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })), Schema.String], { mode: 'oneOf' })
	),
})

export type UpdateSocialIdentityAccessTokenByVerificationIdPayload = { readonly verificationRecordId: string }
export const UpdateSocialIdentityAccessTokenByVerificationIdPayload = Schema.Struct({
	verificationRecordId: Schema.String.check(Schema.isMinLength(1).annotate({ expected: 'a value with a length of at least 1' })),
})

export type UpdateSocialIdentityAccessTokenByVerificationIdResponse = {
	readonly access_token: string
	readonly scope?: string
	readonly token_type?: string
	readonly expires_in?: number | string
}
export const UpdateSocialIdentityAccessTokenByVerificationIdResponse = Schema.Struct({
	access_token: Schema.String,
	scope: Schema.optionalKey(Schema.String),
	token_type: Schema.optionalKey(Schema.String),
	expires_in: Schema.optionalKey(
		Schema.Union([Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })), Schema.String], { mode: 'oneOf' })
	),
})

export type GetEnterpriseSsoIdentityAccessTokenResponse = {
	readonly access_token: string
	readonly scope?: string
	readonly token_type?: string
	readonly expires_in?: number | string
}
export const GetEnterpriseSsoIdentityAccessTokenResponse = Schema.Struct({
	access_token: Schema.String,
	scope: Schema.optionalKey(Schema.String),
	token_type: Schema.optionalKey(Schema.String),
	expires_in: Schema.optionalKey(
		Schema.Union([Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })), Schema.String], { mode: 'oneOf' })
	),
})

export type UpdatePrimaryEmailPayload = { readonly email: string; readonly newIdentifierVerificationRecordId: string }
export const UpdatePrimaryEmailPayload = Schema.Struct({
	email: Schema.String.annotate({ description: 'The new email for the user.', format: 'regex' }).check(
		Schema.isPattern(/^\S+@\S+\.\S+$/).annotate({ expected: 'a string matching the RegExp ^\\S+@\\S+\\.\\S+$' })
	),
	newIdentifierVerificationRecordId: Schema.String.annotate({
		description: 'The identifier verification record ID for the new email ownership verification.',
	}),
})

export type UpdatePrimaryPhonePayload = { readonly phone: string; readonly newIdentifierVerificationRecordId: string }
export const UpdatePrimaryPhonePayload = Schema.Struct({
	phone: Schema.String.annotate({ description: 'The new phone for the user.', format: 'regex' }).check(
		Schema.isPattern(/^\d+$/).annotate({ expected: 'a string matching the RegExp ^\\d+$' })
	),
	newIdentifierVerificationRecordId: Schema.String.annotate({
		description: 'The identifier verification record ID for the new phone ownership verification.',
	}),
})

export type ReplaceIdentityPayload = { readonly newIdentifierVerificationRecordId: string }
export const ReplaceIdentityPayload = Schema.Struct({
	newIdentifierVerificationRecordId: Schema.String.annotate({
		description: 'The identifier verification record ID for the new social identity ownership verification.',
	}),
})

export type AddUserIdentitiesPayload = { readonly newIdentifierVerificationRecordId: string }
export const AddUserIdentitiesPayload = Schema.Struct({
	newIdentifierVerificationRecordId: Schema.String.annotate({
		description: 'The identifier verification record ID for the new social identity ownership verification.',
	}),
})

export type GetMfaVerificationsResponse = ReadonlyArray<{
	readonly id: string
	readonly createdAt: string
	readonly lastUsedAt?: string
	readonly type: 'Totp' | 'WebAuthn' | 'BackupCode' | 'EmailVerificationCode' | 'PhoneVerificationCode'
	readonly agent?: string
	readonly name?: string
	readonly remainCodes?: number
}>
export const GetMfaVerificationsResponse = Schema.Array(
	Schema.Struct({
		id: Schema.String,
		createdAt: Schema.String,
		lastUsedAt: Schema.optionalKey(Schema.String),
		type: Schema.Literals(['Totp', 'WebAuthn', 'BackupCode', 'EmailVerificationCode', 'PhoneVerificationCode']),
		agent: Schema.optionalKey(Schema.String),
		name: Schema.optionalKey(Schema.String),
		remainCodes: Schema.optionalKey(Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' }))),
	})
)

export type AddMfaVerificationPayload =
	| { readonly type: string; readonly newIdentifierVerificationRecordId: string; readonly name?: string }
	| { readonly type: string; readonly secret: string; readonly code?: string }
	| { readonly type: string; readonly codes: ReadonlyArray<string> }
	| { readonly type: 'WebAuthn'; readonly newIdentifierVerificationRecordId: string; readonly name?: string }
	| { readonly type: 'TOTP'; readonly secret: string; readonly code?: string }
	| { readonly type: 'BackupCode'; readonly codes: ReadonlyArray<string> }
export const AddMfaVerificationPayload = Schema.Union(
	[
		Schema.Struct({
			type: Schema.String.annotate({ format: '"WebAuthn"' }),
			newIdentifierVerificationRecordId: Schema.String,
			name: Schema.optionalKey(Schema.String),
		}),
		Schema.Struct({
			type: Schema.String.annotate({ format: '"Totp"' }),
			secret: Schema.String,
			code: Schema.optionalKey(Schema.String),
		}),
		Schema.Struct({ type: Schema.String.annotate({ format: '"BackupCode"' }), codes: Schema.Array(Schema.String) }),
		Schema.Struct({
			type: Schema.Literal('WebAuthn').annotate({ description: 'The type of the MFA verification.' }),
			newIdentifierVerificationRecordId: Schema.String.annotate({
				description: 'The identifier verification record ID for the new WebAuthn registration verification.',
			}),
			name: Schema.optionalKey(
				Schema.String.annotate({
					description: 'The name of the MFA verification, if not provided, the name will be generated from user agent.',
				})
			),
		}),
		Schema.Struct({
			type: Schema.Literal('TOTP').annotate({
				description: 'The type of the MFA verification, for TOTP, one user can only bind one TOTP factor.',
			}),
			secret: Schema.String.annotate({
				description:
					'The TOTP secret for the MFA verification. Use the generate endpoint to create a secret, and verify the generated code with the user before binding to make sure the user has setup the secret in their authenticator app.',
			}),
			code: Schema.optionalKey(
				Schema.String.annotate({ description: 'Optional. The TOTP code generated by the secret to verify the binding.' })
			),
		}),
		Schema.Struct({
			type: Schema.Literal('BackupCode').annotate({
				description:
					'The type of the MFA verification, for backup codes, one user can only bind one set of backup codes and requires at least one other MFA factor.',
			}),
			codes: Schema.Array(Schema.String).annotate({
				description: 'Array of backup codes. Use the generate endpoint to create codes.',
			}),
		}),
	],
	{ mode: 'oneOf' }
)

export type CreateOrReplaceTotpMfaVerificationPayload = { readonly secret: string; readonly code: string }
export const CreateOrReplaceTotpMfaVerificationPayload = Schema.Struct({
	secret: Schema.String.annotate({ description: 'The TOTP secret for the authenticator app.' }),
	code: Schema.String.annotate({ description: 'The TOTP code generated from the secret to confirm the binding.' }),
})

export type UpdateMfaVerificationNamePayload = { readonly name: string }
export const UpdateMfaVerificationNamePayload = Schema.Struct({
	name: Schema.String.annotate({ description: 'The name of the MFA verification.' }),
})

export type GetSessionsResponse = {
	readonly sessions: ReadonlyArray<{
		readonly payload: {
			readonly exp: number
			readonly iat: number
			readonly jti: string
			readonly uid: string
			readonly kind: string
			readonly loginTs: number
			readonly accountId: string
			readonly authorizations?: {
				readonly [x: string]: { readonly sid?: string; readonly grantId?: string; readonly persistsLogout?: boolean }
			}
		}
		readonly lastSubmission:
			| ({
					readonly interactionEvent: 'SignIn' | 'Register' | 'ForgotPassword'
					readonly userId: string
					readonly verificationRecords: ReadonlyArray<
						| {
								readonly id: string
								readonly type: string
								readonly identifier: { readonly type: 'username' | 'email' | 'phone' | 'userId'; readonly value: string }
								readonly verified: boolean
						  }
						| {
								readonly id: string
								readonly templateType:
									| 'SignIn'
									| 'Register'
									| 'ForgotPassword'
									| 'OrganizationInvitation'
									| 'Generic'
									| 'UserPermissionValidation'
									| 'BindNewIdentifier'
									| 'MfaVerification'
									| 'BindMfa'
								readonly verified: boolean
								readonly type: string
								readonly identifier: { readonly type: string; readonly value: string }
						  }
						| {
								readonly id: string
								readonly templateType:
									| 'SignIn'
									| 'Register'
									| 'ForgotPassword'
									| 'OrganizationInvitation'
									| 'Generic'
									| 'UserPermissionValidation'
									| 'BindNewIdentifier'
									| 'MfaVerification'
									| 'BindMfa'
								readonly verified: boolean
								readonly type: string
								readonly identifier: { readonly type: string; readonly value: string }
						  }
						| {
								readonly id: string
								readonly templateType:
									| 'SignIn'
									| 'Register'
									| 'ForgotPassword'
									| 'OrganizationInvitation'
									| 'Generic'
									| 'UserPermissionValidation'
									| 'BindNewIdentifier'
									| 'MfaVerification'
									| 'BindMfa'
								readonly verified: boolean
								readonly type: string
								readonly identifier: { readonly type: string; readonly value: string }
						  }
						| {
								readonly id: string
								readonly templateType:
									| 'SignIn'
									| 'Register'
									| 'ForgotPassword'
									| 'OrganizationInvitation'
									| 'Generic'
									| 'UserPermissionValidation'
									| 'BindNewIdentifier'
									| 'MfaVerification'
									| 'BindMfa'
								readonly verified: boolean
								readonly type: string
								readonly identifier: { readonly type: string; readonly value: string }
						  }
						| {
								readonly id: string
								readonly connectorId: string
								readonly type: string
								readonly socialUserInfo?: {
									readonly id: string
									readonly email?: string
									readonly phone?: string
									readonly name?: string
									readonly avatar?: string
									readonly rawData?: Schema.Json
								}
						  }
						| {
								readonly id: string
								readonly connectorId: string
								readonly type: string
								readonly enterpriseSsoUserInfo?: {
									readonly id: string
									readonly email?: string
									readonly phone?: string
									readonly name?: string
									readonly avatar?: string
									readonly rawData?: Schema.Json
								}
								readonly issuer?: string
						  }
						| { readonly id: string; readonly type: string; readonly userId: string; readonly verified: boolean }
						| { readonly id: string; readonly type: string; readonly userId: string; readonly code?: string }
						| {
								readonly id: string
								readonly verified: boolean
								readonly registrationRpId?: string
								readonly type: string
								readonly userId: string
						  }
						| {
								readonly id: string
								readonly verified: boolean
								readonly registrationRpId?: string
								readonly type: string
								readonly userId?: string
						  }
						| {
								readonly id: string
								readonly type: string
								readonly verified: boolean
								readonly identifier: { readonly type: string; readonly value: string }
								readonly oneTimeTokenContext?: {
									readonly jitOrganizationIds?: ReadonlyArray<string>
									readonly interactionEvent?: 'SignIn' | 'Register' | 'ForgotPassword'
								}
						  }
						| {
								readonly id: string
								readonly type: string
								readonly identifier: { readonly type: 'username' | 'email' | 'phone'; readonly value: string }
						  }
					>
					readonly signInContext?: { readonly [x: string]: string }
			  } & { readonly [x: string]: Schema.Json })
			| null
		readonly clientId: string | null
		readonly accountId: string | null
		readonly expiresAt: number
		readonly isCurrent: boolean
	}>
}
export const GetSessionsResponse = Schema.Struct({
	sessions: Schema.Array(
		Schema.Struct({
			payload: Schema.Struct({
				exp: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				iat: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				jti: Schema.String,
				uid: Schema.String,
				kind: Schema.String.annotate({ format: '"Session"' }),
				loginTs: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				accountId: Schema.String,
				authorizations: Schema.optionalKey(
					Schema.Record(
						Schema.String,
						Schema.Struct({
							sid: Schema.optionalKey(Schema.String),
							grantId: Schema.optionalKey(Schema.String),
							persistsLogout: Schema.optionalKey(Schema.Boolean),
						})
					)
				),
			}),
			lastSubmission: Schema.Union([
				Schema.StructWithRest(
					Schema.Struct({
						interactionEvent: Schema.Literals(['SignIn', 'Register', 'ForgotPassword']),
						userId: Schema.String,
						verificationRecords: Schema.Array(
							Schema.Union(
								[
									Schema.Struct({
										id: Schema.String,
										type: Schema.String.annotate({ format: '"Password"' }),
										identifier: Schema.Struct({
											type: Schema.Union(
												[Schema.Literals(['username', 'email', 'phone']), Schema.Literal('userId')],
												{ mode: 'oneOf' }
											),
											value: Schema.String,
										}),
										verified: Schema.Boolean,
									}),
									Schema.Struct({
										id: Schema.String,
										templateType: Schema.Literals([
											'SignIn',
											'Register',
											'ForgotPassword',
											'OrganizationInvitation',
											'Generic',
											'UserPermissionValidation',
											'BindNewIdentifier',
											'MfaVerification',
											'BindMfa',
										]),
										verified: Schema.Boolean,
										type: Schema.String.annotate({ format: '"EmailVerificationCode"' }),
										identifier: Schema.Struct({
											type: Schema.String.annotate({ format: '"email"' }),
											value: Schema.String,
										}),
									}),
									Schema.Struct({
										id: Schema.String,
										templateType: Schema.Literals([
											'SignIn',
											'Register',
											'ForgotPassword',
											'OrganizationInvitation',
											'Generic',
											'UserPermissionValidation',
											'BindNewIdentifier',
											'MfaVerification',
											'BindMfa',
										]),
										verified: Schema.Boolean,
										type: Schema.String.annotate({ format: '"PhoneVerificationCode"' }),
										identifier: Schema.Struct({
											type: Schema.String.annotate({ format: '"phone"' }),
											value: Schema.String,
										}),
									}),
									Schema.Struct({
										id: Schema.String,
										templateType: Schema.Literals([
											'SignIn',
											'Register',
											'ForgotPassword',
											'OrganizationInvitation',
											'Generic',
											'UserPermissionValidation',
											'BindNewIdentifier',
											'MfaVerification',
											'BindMfa',
										]),
										verified: Schema.Boolean,
										type: Schema.String.annotate({ format: '"MfaEmailVerificationCode"' }),
										identifier: Schema.Struct({
											type: Schema.String.annotate({ format: '"email"' }),
											value: Schema.String,
										}),
									}),
									Schema.Struct({
										id: Schema.String,
										templateType: Schema.Literals([
											'SignIn',
											'Register',
											'ForgotPassword',
											'OrganizationInvitation',
											'Generic',
											'UserPermissionValidation',
											'BindNewIdentifier',
											'MfaVerification',
											'BindMfa',
										]),
										verified: Schema.Boolean,
										type: Schema.String.annotate({ format: '"MfaPhoneVerificationCode"' }),
										identifier: Schema.Struct({
											type: Schema.String.annotate({ format: '"phone"' }),
											value: Schema.String,
										}),
									}),
									Schema.Struct({
										id: Schema.String,
										connectorId: Schema.String,
										type: Schema.String.annotate({ format: '"Social"' }),
										socialUserInfo: Schema.optionalKey(
											Schema.Struct({
												id: Schema.String,
												email: Schema.optionalKey(Schema.String),
												phone: Schema.optionalKey(Schema.String),
												name: Schema.optionalKey(Schema.String),
												avatar: Schema.optionalKey(Schema.String),
												rawData: Schema.optionalKey(Schema.Json.annotate({ expected: 'JSON value' })),
											})
										),
									}),
									Schema.Struct({
										id: Schema.String,
										connectorId: Schema.String,
										type: Schema.String.annotate({ format: '"EnterpriseSso"' }),
										enterpriseSsoUserInfo: Schema.optionalKey(
											Schema.Struct({
												id: Schema.String,
												email: Schema.optionalKey(Schema.String),
												phone: Schema.optionalKey(Schema.String),
												name: Schema.optionalKey(Schema.String),
												avatar: Schema.optionalKey(Schema.String),
												rawData: Schema.optionalKey(Schema.Json.annotate({ expected: 'JSON value' })),
											})
										),
										issuer: Schema.optionalKey(Schema.String),
									}),
									Schema.Struct({
										id: Schema.String,
										type: Schema.String.annotate({ format: '"Totp"' }),
										userId: Schema.String,
										verified: Schema.Boolean,
									}),
									Schema.Struct({
										id: Schema.String,
										type: Schema.String.annotate({ format: '"BackupCode"' }),
										userId: Schema.String,
										code: Schema.optionalKey(Schema.String),
									}),
									Schema.Struct({
										id: Schema.String,
										verified: Schema.Boolean,
										registrationRpId: Schema.optionalKey(Schema.String),
										type: Schema.String.annotate({ format: '"WebAuthn"' }),
										userId: Schema.String,
									}),
									Schema.Struct({
										id: Schema.String,
										verified: Schema.Boolean,
										registrationRpId: Schema.optionalKey(Schema.String),
										type: Schema.String.annotate({ format: '"SignInPasskey"' }),
										userId: Schema.optionalKey(Schema.String),
									}),
									Schema.Struct({
										id: Schema.String,
										type: Schema.String.annotate({ format: '"OneTimeToken"' }),
										verified: Schema.Boolean,
										identifier: Schema.Struct({
											type: Schema.String.annotate({ format: '"email"' }),
											value: Schema.String,
										}),
										oneTimeTokenContext: Schema.optionalKey(
											Schema.Struct({
												jitOrganizationIds: Schema.optionalKey(Schema.Array(Schema.String)),
												interactionEvent: Schema.optionalKey(
													Schema.Literals(['SignIn', 'Register', 'ForgotPassword'])
												),
											})
										),
									}),
									Schema.Struct({
										id: Schema.String,
										type: Schema.String.annotate({ format: '"NewPasswordIdentity"' }),
										identifier: Schema.Struct({
											type: Schema.Literals(['username', 'email', 'phone']),
											value: Schema.String,
										}),
									}),
								],
								{ mode: 'oneOf' }
							)
						),
						signInContext: Schema.optionalKey(Schema.Record(Schema.String, Schema.String)),
					}),
					[Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' }))]
				),
				Schema.Null,
			]),
			clientId: Schema.Union([Schema.String, Schema.Null]),
			accountId: Schema.Union([Schema.String, Schema.Null]),
			expiresAt: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
			isCurrent: Schema.Boolean,
		})
	),
})

export type DeleteSessionByIdParams = { readonly revokeGrantsTarget?: 'all' | 'firstParty' }
export const DeleteSessionByIdParams = Schema.Struct({ revokeGrantsTarget: Schema.optionalKey(Schema.Literals(['all', 'firstParty'])) })

export type GetGrantsParams = { readonly appType?: 'firstParty' | 'thirdParty' }
export const GetGrantsParams = Schema.Struct({ appType: Schema.optionalKey(Schema.Literals(['firstParty', 'thirdParty'])) })

export type GetGrantsResponse = {
	readonly grants: ReadonlyArray<{
		readonly id: string
		readonly payload: {
			readonly exp: number
			readonly iat: number
			readonly jti: string
			readonly kind: string
			readonly clientId: string
			readonly accountId: string
		}
		readonly expiresAt: number
		readonly application: { readonly id: string; readonly name: string }
	}>
}
export const GetGrantsResponse = Schema.Struct({
	grants: Schema.Array(
		Schema.Struct({
			id: Schema.String,
			payload: Schema.Struct({
				exp: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				iat: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
				jti: Schema.String,
				kind: Schema.String.annotate({ format: '"Grant"' }),
				clientId: Schema.String,
				accountId: Schema.String,
			}),
			expiresAt: Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
			application: Schema.Struct({ id: Schema.String, name: Schema.String }),
		})
	),
})

export type UploadAvatarFormData =
	| null
	| string
	| number
	| boolean
	| ({ readonly file?: Schema.Json } & { readonly [x: string]: Schema.Json })
	| ReadonlyArray<Schema.Json>
export const UploadAvatarFormData = Schema.Union([
	Schema.Null,
	Schema.String,
	Schema.Number.check(Schema.isFinite().annotate({ expected: 'a finite number' })),
	Schema.Boolean,
	Schema.StructWithRest(
		Schema.Struct({
			file: Schema.optionalKey(
				Schema.Json.annotate({
					expected: 'JSON value',
					description:
						'The avatar image file to upload. Must be an allowed image type and not exceed the maximum upload file size.',
				})
			),
		}),
		[Schema.Record(Schema.String, Schema.Json.annotate({ expected: 'JSON value' }))]
	),
	Schema.Array(Schema.Json.annotate({ expected: 'JSON value' })),
])

export type UploadAvatarResponse = { readonly url: string }
export const UploadAvatarResponse = Schema.Struct({ url: Schema.String })
