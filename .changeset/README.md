# Changesets

Every user-facing change needs a changeset: run `bun run changeset`, pick the bump, and describe the change for the changelog.

While the package is `0.x`, use `minor` for breaking changes and `patch` for everything else.
